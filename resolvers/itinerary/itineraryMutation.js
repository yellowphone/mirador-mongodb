const createItinerary = (parent, args, context, info) => {
    // context.database.collection('itineraries').findOne().then((data) => {
    //     console.log(data)
    // })
    return "create"
}

module.exports = {
    createItinerary
}